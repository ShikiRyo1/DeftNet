# Examples

`synthetic_demo.py` creates a toy vessel-like image, runs a tiny untrained
DeftNet instance, and writes outputs to `examples_output/`.

The demo is only an API smoke test. It is not a segmentation-quality demo because
the public repository does not currently include trained checkpoints.

```bash
python examples/synthetic_demo.py
```
